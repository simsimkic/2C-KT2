<Window x:Class="InitialProject.View.Tourist.KeyPointView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InitialProject.View.Tourist"
        mc:Ignorable="d"
        Title="KeyPointView" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name = "ShowKeyPoints" ItemsSource="{Binding keyPointsToShow}" Width="700" Margin="13,9,87,197" AutoGenerateColumns="False" SelectionMode="Single" IsReadOnly="True" >

            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Tour" Binding="{Binding Tour}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
